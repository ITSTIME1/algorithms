#include <stdio.h>
#include <vector>
#include <sort>

int main() {
	int integer;
	vector<int> v;
	sort(v.begin(), v.end(), greater_than)
	scanf("%d", &integer);
	printf("%d", integer);
	printf("Hello\n");
	return 0;
}

